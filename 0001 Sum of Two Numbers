class Solution:
    def solve(self, nums, k):
        nums.sort()
        l, r = 0, len(nums)-1
        while l < r:
            if nums[l]+nums[r] > k: r -= 1
            elif nums[l]+nums[r] < k: l += 1
            else: return True
        return False

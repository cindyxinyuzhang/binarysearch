class Solution:
    def solve(self, n):
        ans = list(str(n))
        for i in range(len(ans)):
            if ans[i] != '3':
                ans[i] = '3'
                break
        return int(''.join(ans))
        
